import { NgModule, ModuleWithProviders, Compiler, CompilerOptions } from '@angular/core';
export declare function createJitCompiler(options?: CompilerOptions[]): Compiler;
/**
 * Setup for DynamicComponentDirective
 *
 * ```ts
 * @NgModule({
 *   imports: [
 *     DynamicComponentModule.forRoot({
 *       imports: [CommonModule]
 *     })
 *   ],
 * })
 * class AppModule {}
 * ```
 */
export declare class DynamicComponentModule {
    static forRoot(metadata: NgModule): ModuleWithProviders;
}
